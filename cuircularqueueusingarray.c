#include<stdio.h>
#include<conio.h>
#define size 5
int CQ[size];
int rear = -1, front = -1;
void push(){
	if(rear == front-1 || (rear == size-1 && front == 0)){
		printf("Queue is already full!\n");
		return;
	
	
}
int main(){
	int
}
